<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.nouser666.superdemo">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SuperDemo">
        <activity android:name=".mainview.simplewebview.SimpleWebViewActivity"></activity>
        <activity android:name=".main.MainOnlyTextActivity" />
        <activity android:name=".mainview.rotatevecview.RotateVecViewActivity" />
        <activity android:name=".mainutil.qrencryptionsys.QREncryptionSysActivity" />
        <activity android:name=".maindemo.personcard.PersonCardDemoActivity" />
        <activity android:name=".main.MainActivity" />
        <activity android:name=".StartActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>